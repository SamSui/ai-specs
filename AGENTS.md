---
title: AI 工程化经验知识库 — Agent 使用准则
author: 顾小宇
tags: [AI工程化, 知识库, 指南]
description: 目的：确保 Agent 正确、一致、高效地使用本知识库中的规范与技能 当项目已有明确规范时，遵循项目规范；当项目规范缺失时，引用本知识库的通用规范作为基准。
---

# AI 工程化经验知识库 — Agent 使用准则

> **适用范围**：所有访问和使用本知识库的 AI Agent  
> **目的**：确保 Agent 正确、一致、高效地使用本知识库中的规范与技能

---

## 核心原则

### 1. 规范优先于经验

当项目已有明确规范时，遵循项目规范；当项目规范缺失时，引用本知识库的通用规范作为基准。

### 2. 样本不等于标准

`samples/` 目录中的文档是**脱敏后的项目经验样本**，核心方法论可复用，但具体技术选型、业务术语、人员组织必须按当前项目调整。**禁止直接复制样本中的项目专属内容。**

### 3. 路径是变量

本知识库中所有绝对路径（如 `~/...`）均为**占位符**。使用前必须：
- 确认当前项目的真实路径
- 在任务卡片和 Agent 指令中注入实际路径

### 4. 0号文档是变量

`0号文件`、`需求主文档`、`PRD` 等概念均为**占位符**，指向项目自身的核心需求来源。不同项目的"0号文件"可能不同：
- 可以是 `docs/requirements.md`
- 可以是 `specs/prd.md`
- 可以是外部产品需求文档

**Agent 在使用前必须先确认当前项目的 0号文件实际位置。**

---

## 目录使用权限

| 目录 | 用途 | Agent 权限 |
|------|------|-----------|
| `specs/` | 通用规范（只读引用） | 读取并注入到任务卡片 |
| `rules/` | 评审规则与角色约束 | 读取并按需加载到 Reviewer/Worker |
| `skills/` | 可复用 AI 工作流 | 读取并调度对应 Subagent |
| `tech/` | 技术深度规范 | 读取并作为技术决策参考 |
| `samples/` | 经验样本 | 读取并提炼方法论，**禁止直接复制** |

---

## 规范引用格式

在任务卡片或 Agent 指令中引用本知识库时，使用以下格式：

```markdown
## Knowledge Base
- **项目知识库** → {项目根目录}/specs
- **团队知识库** → {本知识库路径}/specs
- **评审规则** → {本知识库路径}/rules/review-rules.md
- **Reviewer 协议** → {本知识库路径}/specs/reviewer-protocol.md
```

---

## 质量检查清单

在基于本知识库生成产出物前，Agent 必须自检：

- [ ] 是否已确认当前项目的 0号文件位置？
- [ ] 是否已将知识库路径替换为实际路径？
- [ ] 是否去除了样本中的项目专属信息（项目名称、人员、具体技术栈）？
- [ ] 产出的规范是否与项目实际情况对齐？
- [ ] 版本号和修订记录是否已更新？

---

## 更新与反馈

当 Agent 发现以下情况时，应记录并上报：

1. 规范与项目实际情况不匹配
2. 发现新的可复用经验应补充到知识库
3. 样本中的方法论已过时或有更好实践

**上报格式**：
```markdown
[知识库反馈] {规范名称}
- 问题类型: 不适用 / 需补充 / 已过时
- 具体描述: {说明}
- 建议: {改进方向}
```

---

*版本: v1.0*  
*最后更新: 2026-04-29*
